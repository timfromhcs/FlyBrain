"""FlyBrain version metadata (single source of truth for V6)."""
VERSION = "6.0.0"
VERSION_TAG = "v6.0.0"
RELEASE_NAME = "FlyBrain V6.0.0"


def version_string() -> str:
    return f"FlyBrain {VERSION}"
