"""FlyBrain version metadata (single source of truth for V5)."""
VERSION = "5.0.0"
VERSION_TAG = "v5.0.0"
RELEASE_NAME = "FlyBrain V5.0.0"


def version_string() -> str:
    return f"FlyBrain {VERSION}"
