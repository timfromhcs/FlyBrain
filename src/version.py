"""FlyBrain version metadata (single source of truth for V4.1)."""
VERSION = "4.1.0"
VERSION_TAG = "v4.1.0"
RELEASE_NAME = "FlyBrain V4.1.0"


def version_string() -> str:
    return f"FlyBrain {VERSION}"
