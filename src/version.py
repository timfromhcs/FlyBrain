"""FlyBrain version metadata (single source of truth for V10)."""
VERSION = "10.0.0"
VERSION_TAG = "v10.0.0"
RELEASE_NAME = "FlyBrain V10.0.0"
VERSION_INFO = (10, 0, 0)



def version_string() -> str:
    return f"FlyBrain {VERSION}"
